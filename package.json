{
  "name": "quote-discord",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A Discord quote bot with slash commands and local JSON storage.",
  "scripts": {
    "start": "node src/index.js",
    "deploy": "node src/deploy-commands.js",
    "check": "node --check src/index.js && node --check src/commands.js && node --check src/config.js && node --check src/deploy-commands.js && node --check src/format.js && node --check src/storage.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "discord.js": "^14.16.3",
    "dotenv": "^16.4.7"
  }
}
